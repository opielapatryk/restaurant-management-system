apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: menuu
  annotations:
    kubernetes.io/ingress.class: kong
    konghq.com/strip-path: "true"
spec:
  rules:
    - http:
        paths:
          - path: /menuu
            pathType: Prefix
            backend:
              service:
                name: menuu
                port:
                  number: 1027