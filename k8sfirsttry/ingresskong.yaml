apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: menuu
  annotations:
    kubernetes.io/ingress.class: kong
    konghq.com/strip-path: "true"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: menu
                port:
                  number: 1027